# How to configure the development environment

To install the backend dependencies, go to the src directory and run npm install. Make sure that the directory contains a valid package.json file.

To install the frontend (UI) dependencies, navigate to the frontend directory and run npm install to set up everything required for the user interface.

To run the project run the following command: ``npm run start:all``